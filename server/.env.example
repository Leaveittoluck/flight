PORT=5000
DATABASE_URL=postgresql://postgres:password@localhost:5432/postgres
DB_SCHEMA=flight
NODE_ENV=development
PEXELS_API_KEY=your_pexels_api_key_here

# Google OAuth — create credentials at console.cloud.google.com
# Authorised redirect URI must be: http://localhost:5000/api/auth/google/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=

# Session — generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=

# Frontend origin (used for CORS and post-auth redirects)
CLIENT_ORIGIN=
