# Leave empty in local development — the Vite dev server proxy forwards /api/*
# requests to the backend automatically (see vite.config.js server.proxy).
# Production: set to your deployed backend URL (no trailing slash).
VITE_API_BASE_URL=
# Production: VITE_API_BASE_URL=https://your-api.onrender.com
